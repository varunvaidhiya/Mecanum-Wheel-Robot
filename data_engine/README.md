# OmniBot Data Engine

This directory contains the infrastructure for collecting, managing, and processing robot datasets for VLA training.

## Structure
- `schema/`: Definitions of episode formats (HDF5/Parquet).
- `colletors/`: Scripts to transform raw ROS bags into structured episodes.
- `storage/`: Directory for local dataset storage (gitignored).

## Getting Started
(Usage instructions to come)
